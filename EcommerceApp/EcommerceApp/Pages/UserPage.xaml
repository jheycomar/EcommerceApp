<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EcommerceApp.Pages.UserPage"
             Title="UserPage" BackgroundColor="{StaticResource BackgroundColor}"
              BindingContext="{Binding Main, Source={StaticResource Locator}}"
               xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="8" BindingContext="{Binding UserLoget}">
            <Label Text="Welcomen" FontAttributes="Bold" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center"/>
            <Label Text="{Binding FullName}" FontAttributes="Bold" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center"/>           
            <controls:CircleImage  Source="{Binding Photo }" Margin="2" Aspect="AspectFill"
                      WidthRequest="150" HeightRequest="150"
                      HorizontalOptions="Center" VerticalOptions="Center">
            </controls:CircleImage>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>